# Frontend API Base URL
# - Development: http://localhost:5000 (or leave empty if using Vite proxy)
# - Production: leave empty (nginx will proxy /api/* to backend)
VITE_API_BASE_URL=

# Allowed Hosts for Vite (comma-separated for multiple hosts)
VITE_ALLOWED_HOSTS=

# JWT Secret Key (change this in production)
JWT_SECRET=dev-jwt-secret-key-2025

# Static Authentication Token (any token that matches this will be accepted)
STATIC_AUTH_TOKEN=dev-auth-token-2025

# JWT Expiration Hours
JWT_EXPIRATION_HOURS=24

# Maximum file size in bytes (500MB)
MAX_CONTENT_LENGTH=524288000